# 浮生流年 — AI 小说写作平台设计文档

> 2026-07-22 架构更新：DeerFlow 已移除，生产流水线由项目 durable worker
> 自行编排并直连 OpenAI 兼容模型。本文后续 DeerFlow 章节仅保留为历史设计记录，
> 当前实现与部署以 `README.md` 和代码为准。

> 项目代号：fushengliunian  
> 版本：v3.0（生产优化版 — 集成活体 Wiki / PageIndex / 叙事学协议）  
> 日期：2026-07-19  
> 目标：让不懂技术的写作者，通过简单引导，用 AI 写出几百万字不跑偏、像真人写的长篇小说  
> 设计基准：500 万字 / 1250 章 / 200 角色 / 500 伏笔线程  
> v3.0 新增：活体故事 Wiki + TOC 树检索 + 9 卡叙事学协议 + 计算级去 AI 味 + 6 门验证

---

## 一、产品目标与能力边界

系统围绕长篇小说持续生产建立以下核心能力：

| 能力 | 本项目实现 |
|------|------------|
| 分层规划 | 一句话构想 → 全书大纲 → 卷纲 → 章纲 → 正文 |
| 多角色流水线 | 建筑师、写手、编辑、连续性守卫、观察者和风格处理器协作 |
| 状态一致性 | 追加式状态事件、Current State 投影、实体引用和世界规则校验 |
| 长篇检索 | PostgreSQL FTS、pgvector、实体检索与分层 PageIndex 融合 |
| 活体知识库 | Wiki ingest、query、lint 三操作循环和不可变版本历史 |
| 质量控制 | 确定性检查与语义检查组成九道发布门 |
| 持久任务 | 数据库领取、租约心跳、崩溃恢复、节点幂等和暂停恢复 |

多 Agent 编排采用 DeerFlow 开源框架，通过项目自有 skills 实现小说生产流程。DeerFlow 的版权和 MIT 许可证保持在 `backend/deerflow/LICENSE`，其他第三方组件见 `THIRD_PARTY_NOTICES.md`。

---

## 二、核心设计理念

### 2.1 给傻瓜用的前端，给天才用的后端

前端：**向导式**。用户不需要理解"大纲""世界观""角色卡"这些概念。系统用自然语言问他几个问题，后台自动构建一切。

后端：通过结构化状态事件和确定性连续性检查，保障百万字规模下的事实一致性。

### 2.2 像真人写作的五个关键

1. **状态驱动，不靠记忆**：每章生成前读取结构化状态快照，不依赖 LLM 的上下文窗口"记住"前文
2. **活体 Wiki，自维护一致性**：故事圣经不是静态文档，而是每章自动 ingest、定期 lint 的活体 Wiki
3. **叙事学驱动写作**：用 9 卡叙事学协议约束写作质量，包括聚焦化边界、心理距离和场景因果律
4. **计算级去 AI 味**：不只是词表替换，而是测量突发性（句长标准差）、语义困惑度（连续句功能重复率）、结构指纹（模板连接词）等计算指标
5. **POV 投影**：世界在模拟层是完整的，但每个场景只把视角人物能感知的内容注入 prompt

### 2.3 DeerFlow 作为编排层

不重新造轮子。用 DeerFlow 的 Skill 系统定义写作工作流，用它的多 Agent 编排调度写作流水线中的各个角色。

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户浏览器                                │
│                  novel.example.com                           │
│         ┌──────────────────────────────────┐                     │
│         │   前端 (Next.js / 静态导出)       │                     │
│         │   向导引导 → 工作台 → 阅读预览    │                     │
│         └──────────────┬───────────────────┘                     │
└────────────────────────┼─────────────────────────────────────────┘
                         │ HTTPS (Nginx 反代)
┌────────────────────────┼─────────────────────────────────────────┐
│  阿里云 Ubuntu 服务器                                           │
│                        │                                         │
│  ┌─────────────────────▼──────────────────────┐                  │
│  │        API Gateway (FastAPI)                │                  │
│  │   用户认证 / 项目管理 / SSE 推送 / 文件服务  │                  │
│  │              :8100                          │                  │
│  └─────────────────────┬──────────────────────┘                  │
│                        │                                         │
│  ┌─────────────────────▼──────────────────────┐                  │
│  │       DeerFlow 写作引擎 (核心)              │                  │
│  │                                            │                  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐   │                  │
│  │  │ 建筑师   │ │ 写手     │ │ 编辑     │   │                  │
│  │  │Architect │ │ Writer   │ │ Editor   │   │                  │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘   │                  │
│  │       │            │            │          │                  │
│  │  ┌────▼─────┐ ┌────▼─────┐ ┌───▼──────┐  │                  │
│  │  │ 连续性   │ │ 风格     │ │ 去AI味   │  │                  │
│  │  │Guardian  │ │ Stylist  │ │Humanizer │  │                  │
│  │  └──────────┘ └──────────┘ └──────────┘  │                  │
│  │                                           │                  │
│  │  ┌────────────────────────────────────┐   │                  │
│  │  │        记忆引擎 Memory Engine       │   │                  │
│  │  │  状态快照 + 故事圣经 + 混合检索     │   │                  │
│  │  └────────────────────────────────────┘   │                  │
│  └────────────────────────────────────────────┘                  │
│                        │                                         │
│  ┌─────────────────────▼──────────────────────┐                  │
│  │         PostgreSQL (fushengliunian 库)      │                  │
│  │   用户/项目/章节/实体/状态/记忆向量          │                  │
│  └────────────────────────────────────────────┘                  │
│                                                                  │
│  ┌────────────────────────────────────────────┐                  │
│  │              Nginx 反代入口                 │                  │
│  │   novel.example.com → :8100            │                  │
│  └────────────────────────────────────────────┘                  │
└──────────────────────────────────────────────────────────────────┘
                         │
                    外部 LLM API
          ┌──────────┬──────────┬──────────┐
          │ DeepSeek  │ 豆包     │ Claude   │
          │ (主力)    │ (备选)   │ (高质量) │
          └──────────┴──────────┴──────────┘
```

### 3.1 为什么这么设计

**服务器只有 2GB 内存**，必须精打细算：

- **不在服务器跑 DeerFlow 的 Docker 沙盒**。DeerFlow 的 Coder/Researcher 沙盒不需要——我们只用它的 LangGraph 编排能力和 Skill 系统，直接以 Python 进程方式运行
- **PostgreSQL 复用现有实例**，新建 `fushengliunian` 库，不额外起 MinIO
- **前端静态导出**，Nginx 直接托管，零内存开销
- **LLM 调用全走外部 API**（DeepSeek/豆包/Claude），服务器只做编排
- FastAPI + DeerFlow 引擎合并为单进程，预估内存占用 200-400MB

---

## 四、记忆引擎设计（核心竞争力）

这是让百万字小说不跑偏的关键。综合 8 个项目的最佳实践，设计三层记忆架构：

### 4.1 三层记忆 + 冷热分层架构

> **压力测试发现**：原设计中 L2 状态快照在 500 章时膨胀到约 ~500K tokens（200 角色
> × 状态/位置/物品/认知 + 500 条伏笔 + 势力/地点/时间线），而 Context Pack 只有
> 8K tokens，相当于只能看到全部状态的 1.3%。200 章后系统实质上"看不见"大部分世界。
> 
> **解决方案**：引入冷热分层，只有"活跃"实体进入 Context Pack。
>
> **v3.0 升级**：L1 从被动"故事圣经"升级为可持续维护的"活体故事 Wiki"，
> 每章自动 ingest 更新，定期 lint 检测矛盾/孤页/陈旧声明。

```
┌─────────────────────────────────────────────────────────────┐
│           L1: 活体故事 Wiki (Living Story Wiki)              │
│     ★ v3.0: 从被动圣经升级为 LLM-Wiki 模式的活体知识库       │
│                                                             │
│  每个实体一个 Wiki 页面，用 [[wikilink]] 互相引用：           │
│                                                             │
│  wiki/                                                      │
│  ├── characters/                                            │
│  │   ├── 李逍遥.md        # 角色档案 + 动态状态 + 关系网    │
│  │   ├── 灵儿.md                                            │
│  │   └── ...                                                │
│  ├── locations/                                             │
│  │   ├── 青云城.md         # 地点描述 + 当前状态 + 历史事件  │
│  │   └── 万妖山.md                                          │
│  ├── factions/                                              │
│  │   └── 青云宗.md         # 势力档案 + 成员 + 关系         │
│  ├── worldview/                                             │
│  │   ├── 修炼体系.md       # 世界规则                       │
│  │   └── 禁忌规则.md       # canon rules，不可违反          │
│  ├── timeline/                                              │
│  │   └── 主时间线.md       # 按时间排列的关键事件            │
│  └── _wiki_schema.json     # Wiki 结构定义（页面类型+字段）  │
│                                                             │
│  核心三操作循环:                                             │
│                                                             │
│  ┌──────────────────────────────────────────────┐           │
│  │ INGEST (每章完成后自动执行)                    │           │
│  │                                              │           │
│  │ 1. 读取 Observer 的 CHANGES 声明              │           │
│  │ 2. 更新涉及实体的 Wiki 页面                    │           │
│  │    - 角色页: 更新状态/位置/关系/认知            │           │
│  │    - 地点页: 更新当前状态/发生事件              │           │
│  │    - 时间线: 追加事件条目                       │           │
│  │ 3. 检查是否需要新建页面                        │           │
│  │    (新出场的角色/新发现的地点/新建的势力)       │           │
│  │ 4. 更新 [[wikilink]] 交叉引用                 │           │
│  │ 5. 附带来源标注: [来源: 第42章]                │           │
│  └──────────────────────────────────────────────┘           │
│                                                             │
│  ┌──────────────────────────────────────────────┐           │
│  │ QUERY (Context Pack 组装时调用)                │           │
│  │                                              │           │
│  │ 根据 beat sheet 中的实体名，                    │           │
│  │ 直接读取对应 Wiki 页面注入 Context Pack         │           │
│  │ → 比从 state_log 聚合更快、更结构化            │           │
│  └──────────────────────────────────────────────┘           │
│                                                             │
│  ┌──────────────────────────────────────────────┐           │
│  │ LINT (每 10 章自动 + 每 50 章深度)             │           │
│  │                                              │           │
│  │ 结构检查 (零 token, 代码执行):                  │           │
│  │ · 孤页检测: Wiki 页面无任何 [[入链]]            │           │
│  │ · 断链检测: [[wikilink]] 指向不存在的页面       │           │
│  │ · 陈旧检测: 页面 last_updated_chapter 落后     │           │
│  │   当前章 > 50 章                               │           │
│  │ · 缺页检测: 正文频繁提及但无 Wiki 页面的实体    │           │
│  │                                              │           │
│  │ 语义检查 (LLM, 每 50 章深度 lint):              │           │
│  │ · 矛盾检测: 比较标签重叠的页面对，              │           │
│  │   检查事实冲突 (如 A 页说角色在东，B 页说在西)  │           │
│  │ · 陈旧声明: 被后续章节推翻但 Wiki 未更新的事实  │           │
│  │ · 概念缺失: 重要概念频繁出现但无独立页面        │           │
│  │                                              │           │
│  │ 输出: lint_report → 自动修复结构问题,           │           │
│  │       语义问题生成修正建议供用户确认             │           │
│  └──────────────────────────────────────────────┘           │
│                                                             │
│  存储: PostgreSQL story_wiki 表 (替代原 story_bible)         │
│  每页字段: slug, category, title, content(markdown),         │
│           wikilinks[], last_updated_chapter, source_chapters │
│  更新频率: 每章自动 ingest + 每 10 章结构 lint               │
└─────────────────────────────────────────────────────────────┘
                         │
┌─────────────────────────────────────────────────────┐
│            L2: 状态快照 (State Snapshot)              │
│       冷热分层：热区 ≤ 50 实体 + 冷区归档              │
│                                                     │
│  ┌─── 热区（注入 Context Pack）──────────────┐      │
│  │ 最近 20 章出场的角色状态                    │      │
│  │ 当前弧线活跃的冲突/伏笔                     │      │
│  │ 视角角色直接关联的关系网                     │      │
│  │ 当前地点 + 相邻地点状态                     │      │
│  │ 体积上限: ≤ 3000 tokens                    │      │
│  └────────────────────────────────────────────┘      │
│                                                     │
│  ┌─── 冷区（按需检索）────────────────────────┐      │
│  │ 超过 20 章未出场的角色 → 冷冻               │      │
│  │ 已完结的冲突/已回收的伏笔 → 归档            │      │
│  │ 前卷的势力/地点状态 → 压缩摘要              │      │
│  │ 升温条件: 被章纲提及 / 被 RAG 命中          │      │
│  └────────────────────────────────────────────┘      │
│                                                     │
│  15 维跟踪不变:                                      │
│  角色状态 │ 角色位置 │ 角色外貌变化 │ 冲突进度         │
│  伏笔状态 │ 情节节点 │ 地点状态 │ 势力状态             │
│  时间线 │ 物品状态 │ 世界约束 │ 秘密                   │
│  誓约 │ 截止期限 │ 角色认知(谁知道什么)                │
│                                                     │
│  每条记录新增字段:                                    │
│    last_referenced_chapter: 最后被提及的章节号         │
│    temperature: hot/warm/cold                        │
│    confidence: 0-1 (被后续章节佐证的次数越多越高)      │
│                                                     │
│  存储: PostgreSQL，append-only 日志 + 物化当前视图    │
│  更新: CHANGES 声明 → 追加日志 → 重建物化视图         │
└─────────────────────────────────────────────────────┘
                         │
┌─────────────────────────────────────────────────────┐
│            L3: 章节记忆 (Chapter Memory)              │
│       已写章节的摘要和全文索引                         │
│                                                     │
│  章节摘要链 │ 情节关键事件 │ 伏笔埋设/回收记录        │
│  全文分块向量索引 │ FTS 关键词索引                    │
│                                                     │
│  分块时附加元数据标签:                                │
│    涉及角色 │ 所属弧线 │ 章节号范围                   │
│  检索时先按元数据过滤，再做相似度排序                  │
│                                                     │
│  存储: PostgreSQL chapters 表 + pgvector 扩展        │
│  检索: 元数据预过滤 → 混合搜索 (BM25 + 向量 + 实体)  │
└─────────────────────────────────────────────────────┘
```

**冷热升降温规则：**

```
升温 (cold → hot):
  - 被下一章的章纲点名提及
  - 被 RAG 检索命中且相似度 > 0.8
  - 被活跃伏笔关联
  - 距离上次出场 < 20 章

降温 (hot → cold):
  - 连续 20 章未出场且未被任何活跃伏笔关联
  - 角色死亡/退场（标记为 frozen，永不自动升温）
  - 冲突已完结
  
自动压缩:
  - 每完成一卷（约 100 章），对该卷的冷区做 LLM 压缩
  - 把详细状态压缩为 200 字摘要，保留关键事实
  - 原始详细状态保留在数据库但不再进入检索
```

### 4.2 每章生成的上下文组装（Context Pack）

Context Pack 采用分层 Token 预算，并按事实相关性和时效性进行优先级裁剪。

> **压力测试发现**：固定 8K token 预算在 800 章时严重不足。一个涉及 5 个角色、
> 3 条活跃伏笔的场景，仅角色状态+伏笔+近章摘要就需要 8K，RAG 和世界规则完全被裁剪。
>
> **解决方案**：动态预算 + 场景相关性预筛选。

```
Token 预算: 动态 8K-16K（由场景复杂度决定）

场景复杂度评估（在 Architect 输出 beat sheet 后计算）：
  出场角色 ≤ 3 且 活跃伏笔 ≤ 2 → 简单场景 → 8K tokens
  出场角色 4-6 或 活跃伏笔 3-5 → 标准场景 → 12K tokens
  出场角色 > 6 或 活跃伏笔 > 5 或 大战/转折 → 复杂场景 → 16K tokens

分配策略（按优先级从高到低，超预算时从底部裁剪）：

优先级 0 - 任务指令 (5%)          : 本章写作任务描述
优先级 1 - 禁忌规则 (5%)          : canon rules，不可违反的设定
优先级 2 - 本章大纲 (5%)          : 本章要写什么
优先级 3 - 场景相关状态切片 (15%) : ★ 不是全量快照，而是由预筛选器
                                    根据 beat sheet 提取的"本场景需要
                                    知道什么"——只含出场角色的状态、
                                    本场景地点、关联冲突
优先级 4 - 前一章尾段 (10%)       : 最近一章的最后 2000 字
优先级 5 - 出场角色档案 (10%)     : 仅本章出场角色的档案（不是全部）
优先级 6 - 近 3 章摘要 (10%)      : 前 3 章的结构化摘要
优先级 7 - 到期伏笔 (8%)          : 本章应回收的伏笔 + 近期到期的
优先级 8 - RAG 检索结果 (12%)     : ★ 带元数据预过滤的混合检索
优先级 9 - 下一章预览 (5%)        : 下一章大纲，用于衔接铺垫
优先级10 - 风格指南 (5%)          : 写作风格/禁用词/对话比例等
优先级11 - 历史里程碑 (5%)        : 全书关键转折点摘要
优先级12 - 冷区按需召回 (5%)      : 被 RAG 命中的冷区实体摘要

★ = 与原设计的关键区别
```

**"场景相关性预筛选器"详细逻辑：**

```python
def build_scene_slice(beat_sheet, full_snapshot):
    """从 beat sheet 提取本场景需要的状态切片，而非注入全量快照"""
    
    # 1. 从 beat sheet 提取出场角色名单
    characters = extract_characters(beat_sheet)
    
    # 2. 只取这些角色的状态（不是全部 200 个角色）
    char_states = {c: full_snapshot.characters[c] 
                   for c in characters if c in full_snapshot.characters}
    
    # 3. 只取本场景的地点状态
    location = extract_location(beat_sheet)
    loc_state = full_snapshot.locations.get(location)
    
    # 4. 只取与出场角色相关的活跃冲突
    conflicts = [c for c in full_snapshot.conflicts 
                 if c.status == 'active' 
                 and any(char in c.participants for char in characters)]
    
    # 5. 只取出场角色之间的关系
    relationships = get_relationships_between(characters, full_snapshot)
    
    # 6. 只取出场角色的认知（谁知道什么）
    knowledge = {c: full_snapshot.knowledge[c] for c in characters}
    
    return SceneSlice(char_states, loc_state, conflicts, relationships, knowledge)
```

### 4.3 CHANGES 声明机制（双重提取 + 追溯修正）

每章生成后，AI 必须输出可验证、可追溯的结构化状态变更。

> **压力测试发现**：LLM 对结构化提取的遗漏率约 5-15%。500 章累计 250-750 条
> 遗漏的状态变更。最危险的是"隐式变更"——角色在对话中无意间获知信息、物品在战斗中
> 损毁但未显式提及。一旦遗漏，错误在后续章节中被"合法化"并不断放大（级联崩溃）。
>
> **解决方案**：双重提取 + append-only 日志 + 置信度评分。

**双重提取流程：**

```
章节正文生成完毕
    │
    ▼
第一遍：Observer Agent 输出 CHANGES 声明
    │
    ▼
第二遍：Verifier Agent（不同 prompt，独立调用）
    "仔细阅读以下章节正文，列出所有状态变更，
     包括隐式的（角色无意间获知的信息、战斗中
     损坏的物品、未明确描写但可推断的位置移动）"
    │
    ▼
Diff 对比：
    两遍结果取并集
    冲突项标记为 confidence: 0.5（待人工确认或后续章节佐证）
    一致项标记为 confidence: 0.9
```

**CHANGES 数据结构：**

```json
{
  "chapter_id": 42,
  "changes": {
    "character_state": [
      {"name": "李逍遥", "field": "修为", "old": "筑基三层", "new": "筑基六层",
       "confidence": 0.9, "evidence": "正文第3段明确描写突破"}
    ],
    "character_position": [
      {"name": "李逍遥", "from": "青云城", "to": "万妖山",
       "confidence": 0.9}
    ],
    "conflict_progress": [
      {"conflict": "争夺天灵果", "progress": "60%", "note": "李逍遥击败外门弟子",
       "confidence": 0.9}
    ],
    "foreshadowing": [
      {"action": "plant", "content": "山洞中发现的古符文", "target_chapter": null},
      {"action": "resolve", "ref_chapter": 35, "content": "老者的身份揭晓"}
    ],
    "time_advance": {"amount": "3天", "current": "灵历1342年秋·九月初七"},
    "item_transfer": [
      {"item": "碧火剑", "from": "青云宗藏剑阁", "to": "李逍遥",
       "confidence": 0.9}
    ],
    "secret_reveal": [
      {"secret": "长老的叛徒身份", "revealed_to": ["李逍遥"], "still_hidden_from": ["宗主"],
       "confidence": 0.9}
    ],
    "implicit_knowledge": [
      {"character": "灵儿", "learned": "李逍遥会碧火剑法",
       "source": "目睹战斗", "confidence": 0.7,
       "note": "Verifier 独立发现，Observer 遗漏"}
    ]
  }
}
```

**Append-Only 状态日志（防级联崩溃）：**

```
核心原则：永远不覆写，只追加。

state_log 表（每条 CHANGES 中的每个变更一条记录）：
  id | project_id | chapter_seq | dimension | entity_name 
     | field | old_value | new_value | confidence | source

当前状态 = 对 state_log 按 chapter_seq 降序取每个 entity+field 的最新值
         （物化视图，每章生成后刷新）

级联崩溃修复流程：
  1. 发现第 300 章的 CHANGES 遗漏了"角色 A 的剑在战斗中断了"
  2. 插入修正记录: chapter_seq=300, field="武器", new_value="断剑"
  3. 扫描第 301-当前章，找出所有引用"角色 A 的剑"的章节
  4. 标记这些章节为"待审核"（不自动重写，交给用户决定）
  5. 后续生成自动使用修正后的状态
```

### 4.4 连续性引擎（三层检查）

连续性引擎由确定性检查和语义检查共同组成。

> **压力测试发现**：代码级检查只能抓"机械性"错误。无法检测：
> - 人设漂移（角色突然性格大变，没有铺垫）
> - 能力遗忘（角色不用已有的强力能力，犯低级错误）
> - 战略弱智（反派明明有更好的选择却用最蠢的方法）
> - 节奏退化（连续 10 章都是打怪升级，无变化）
> - 契诃夫之枪（引入的元素从不使用）
>
> **解决方案**：三层检查 = 代码级 + Guardian LLM 级 + 定期审计。

```
┌─────────────────────────────────────────────────────────┐
│  第一层：代码级检查（每章，零 token）                     │
│                                                         │
│  1. 死角色复活检测                                       │
│  2. 位置瞬移检测（无移动描写的位置跳变）                  │
│  3. 物品持有合法性（使用不在身上的物品）                   │
│  4. 伏笔超时预警（埋设超 N 章未回收，N 按总章数动态调整） │
│  5. 主角失踪预警（连续 3 章未出场）                       │
│  6. 时间线倒流检测                                       │
│  7. 信息边界违规（角色使用不应知道的信息）                │
│  8. 称谓一致性                                           │
│  9. ★ 低置信度状态预警（confidence < 0.7 的状态被引用）  │
│ 10. ★ 冷区实体意外出场（冷区角色未经升温就出现在正文中） │
└─────────────────────────────────────────────────────────┘
                         │ 结果注入
                         ▼
┌─────────────────────────────────────────────────────────┐
│  第二层：Guardian Agent LLM 检查（每章）                  │
│                                                         │
│  输入: beat sheet + 代码检查结果 + 出场角色人设           │
│  检查:                                                   │
│  1. 逻辑合理性（因果链是否通顺）                         │
│  2. 人设一致性（行为是否符合角色性格）                    │
│  3. 能力使用合理性（是否遗忘已有能力）                    │
│  4. 信息合理性（角色决策是否基于其已知信息）              │
│  输出: PASS / FAIL + 具体问题描述                        │
│  FAIL → 返回 Architect 修改 beat sheet（最多 3 轮）      │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  第三层：定期深度审计（每 50 章一次）                     │
│                                                         │
│  ★ 这是原设计完全没有的新机制                            │
│                                                         │
│  用长上下文模型（Claude 200K 或 DeepSeek 128K），输入:   │
│  - 全部角色档案                                         │
│  - 最近 50 章的结构化摘要                                │
│  - 活跃伏笔池完整列表                                   │
│  - 当前弧线的预期走向                                    │
│                                                         │
│  审计维度:                                               │
│  1. 人设漂移检测：角色行为模式是否偏离初始设定            │
│  2. 能力一致性：引入的能力/道具是否被合理使用             │
│  3. 节奏分析：连续 N 章的情节节奏是否单调                │
│  4. 伏笔健康度：是否有伏笔埋了太久没回收                 │
│  5. 叙事张力曲线：是否持续有冲突和悬念                   │
│  6. 契诃夫之枪：引入的重要元素是否都有用途               │
│  7. 反派智商：反派的决策是否合理                          │
│                                                         │
│  输出: 审计报告 → 生成"修正指令"注入后续章节大纲         │
│  成本: 每次约 ¥1-3（50 章一次，可接受）                  │
└─────────────────────────────────────────────────────────┘
```

### 4.5 伏笔生命周期管理

> **压力测试发现**：500+ 活跃伏笔无法管理。平均等待 200+ 章回收，读者早已忘记。
> 系统试图把全部活跃伏笔注入 Context Pack 会挤占其他预算。
>
> **解决方案**：硬性上限 + 优先级队列 + 强制升级机制。

```
伏笔管理规则：

1. 活跃伏笔硬上限: 50 条
   - 超过 50 条时，新埋伏笔必须同时指定一条旧伏笔为"计划回收"
   - 或由系统自动将最旧的 5 条标记为"加速回收"

2. 优先级队列:
   priority = 1.0 / (当前章 - 埋设章) × 重要度权重
   
   重要度分级:
   - S级 (权重 3.0): 主线伏笔（与主角命运/终极目标直接相关）
   - A级 (权重 2.0): 重要支线伏笔（与卷级冲突相关）
   - B级 (权重 1.0): 氛围/彩蛋伏笔

3. 到期强制升级:
   - 伏笔超过 target_chapter 的 80% → 注入到后续 5 章的大纲提示中
   - 超过 target_chapter → 强制写入下一章的 beat sheet
   - 超过 target_chapter + 20% 仍未回收 → 自动生成一个"回声场景"
     （简短回顾/暗示，让读者记起这个伏笔）
   - 超过 target_chapter + 50% → 标记为 abandoned，从活跃池移除
     并在 Observer 日志中记录（供审计报告使用）

4. Context Pack 中只注入:
   - 本章需要回收的伏笔（完整内容）
   - 近 5 章到期的伏笔（简要提示）
   - 最多 10 条，按优先级排序
```

### 4.6 双轨检索系统（TOC 树 + 混合搜索）

> **v2.0 问题**：纯向量检索噪声大（同题材语义相似），且无法回答结构性问题
> （"角色 A 第一次出现是什么时候""这个伏笔最早在哪里埋的"）。
>
> **v3.0 升级**：引入 PageIndex 的 TOC 树推理导航，与原有混合检索组成双轨系统。
> TOC 树擅长结构性查询和跨章回溯；混合搜索擅长语义相似检索。

```
                      ┌─── 检索请求 ───┐
                      │                │
              ┌───────▼──────┐  ┌──────▼───────┐
              │  轨道 A:      │  │  轨道 B:      │
              │  TOC 树导航   │  │  混合搜索      │
              │  (PageIndex)  │  │  (v2.0 已有)   │
              └───────┬──────┘  └──────┬───────┘
                      │                │
                      ▼                ▼
              ┌──────────────────────────────┐
              │    结果合并 + 去重 + 排序     │
              └──────────────────────────────┘
                           │
                           ▼
                    注入 Context Pack

═══════════════════════════════════════════════

轨道 A: TOC 树推理导航 (PageIndex 模式)

  小说的 TOC 树结构（自动构建，每章完成后增量更新）：
  
  Novel
  ├── Volume 1: 入门篇 (ch1-100)
  │   ├── Arc 1.1: 青云试炼 (ch1-30)
  │   │   ├── ch1: {标题, 摘要, 出场角色, 关键事件}
  │   │   ├── ch2: ...
  │   │   └── ch30: ...
  │   └── Arc 1.2: 万妖山历险 (ch31-100)
  │       └── ...
  └── Volume 2: 成长篇 (ch101-250)
      └── ...

  TOC 树存储: PostgreSQL novel_toc 表
  每节点: node_id, parent_id, level(volume/arc/chapter),
          title, summary(≤200字), characters[], key_events[],
          chapter_range_start, chapter_range_end

  检索流程:
  1. 将 TOC 树 JSON (仅标题+摘要, ~2000-4000 tokens) 注入 LLM
  2. LLM 推理: "要回答这个查询，应该看哪些节点？"
  3. 返回 ranked node_ids → 读取对应章节分块
  4. 如果上下文不足 → 迭代扩展到相邻节点

  适用场景:
  · 结构性查询: "角色 A 第一次出现在哪章"
  · 跨卷回溯: "这个伏笔最早在哪里埋的"
  · 弧线级上下文: "这条支线的整体发展脉络"
  · 审计用: "最近 3 卷中角色的行为模式变化"

═══════════════════════════════════════════════

轨道 B: 元数据预过滤混合搜索 (v2.0 优化版)

  查询 → 元数据预过滤（零成本）
         │
         │ 过滤条件（从 beat sheet 自动提取）：
         │ - 涉及角色 ∩ 分块标记角色（Aho-Corasick 零成本匹配）
         │ - 章节范围: 优先最近 50 章 + 关键转折章节
         │ - 弧线标签: 当前弧线相关
         │
         ▼ 过滤后候选集（通常 500-2000 条）
         │
         ├→ 关键词搜索 (PostgreSQL FTS) ─────→ BM25 打分
         ├→ 向量搜索 (pgvector) ──────────────→ 余弦相似度
         └→ 实体匹配 (Aho-Corasick) ─────────→ 实体相关度
                 │
                 ▼
         Reciprocal Rank Fusion (RRF, k=60)
         加权: 关键词=1.0, 向量=0.8, 实体=1.2
                 │
                 ▼
            Top-K 结果

═══════════════════════════════════════════════

路由策略:
  · 章节生成时的 Context Pack 组装 → 仅用轨道 B（速度快，够用）
  · Guardian/Auditor 检查 → 用轨道 A（需要跨章回溯）
  · 伏笔回收查询 → 用轨道 A（需要找到原始埋设位置）
  · 用户主动搜索 → 双轨并行，合并结果
```

---

## 五、写作流水线设计

### 5.1 总体流程（用户视角）

```
用户打开网站
    │
    ▼
第一步：回答几个问题（向导）
    "你想写什么类型的故事？" → 选择题
    "用一句话描述你的故事" → 输入框
    "主角叫什么？什么性格？" → 输入框
    "大概写多长？" → 选择题
    │
    ▼
系统自动生成（后台，用户等待 1-2 分钟）
    世界观 → 角色卡 → 大纲 → 卷纲 → 章纲
    │
    ▼
用户预览大纲（可修改）
    │
    ▼
点击"开始写作"
    │
    ▼
逐章生成（每章 10-30 秒）
    用户可以：
    - 看生成进度（SSE 流式）
    - 暂停/继续
    - 回退重写某章
    - 修改后续大纲
    - 导出已写内容
```

### 5.2 后台 Agent 流水线

每章经过 7 个 Agent 处理，由 DeerFlow 编排。
v3.0 核心升级：Architect 引入微弧节奏、Writer 引入叙事学 9 卡协议、
Humanizer 升级为计算级检测、Observer 拆为 3 路并行提取。

```
┌──────────────────────────────────────────────────────────────┐
│                  章节生成流水线 (v3.0)                         │
│                                                              │
│  ① 建筑师 Architect                                         │
│     输入: 章纲 + Wiki 页面(出场实体) + 前章摘要               │
│     输出: 场景拍 (beat sheet) + 微弧节奏分配                  │
│                                                              │
│     ★ v3.0 微弧节奏:                                        │
│     每章强制 4 段式结构:                                      │
│     · 开场触发 (15-20%): 承接上章，引入本章事件               │
│     · 推进冲突 (40-50%): 至少 2 个冲突(1外部+1内心)           │
│     · 逆转/新信息 (20-25%): 改变局势的转折                   │
│     · 余波/钩子 (10-15%): 情感落点 + 下章悬念                 │
│                                                              │
│     ★ 禁止模板化表述:                                       │
│     ban: "开场建立章节目标"、"中段制造冲突"、                 │
│          "结尾留下悬念" 等元叙事描述                          │
│     rule: 每个 beat 必须是"具体事件"，有角色动作和情势变化     │
│     rule: 不得在单章内解决所有核心矛盾                        │
│                                                              │
│  ② 连续性守卫 Guardian (代码级 + LLM 级)                     │
│     (v2.0 已有，v3.0 不变)                                    │
│     代码检查: 10 项零 token 检查                              │
│     LLM 检查: 逻辑/人设/能力/信息合理性                      │
│     输出: 通过 ✓ 或 修改建议 → 返回 ①（最多 3 轮）           │
│                                                              │
│  ③ 写手 Writer                                              │
│     输入: 通过审核的 beat sheet + Context Pack                │
│                                                              │
│     ★ v3.0 叙事学 9 卡协议:                                 │
│     注入 Writer prompt 的 9 条叙事学约束:                     │
│                                                              │
│     卡1 聚焦化边界: 谁能知道什么？视角人物不能读取他人内心     │
│         违规=硬错误，自动触发重写                              │
│     卡2 心理距离: 决定/受伤/发现 → 近距离(内心独白)           │
│         跨时间背景 → 远距离(概述)                              │
│     卡3 场景 vs 概述: 每个场景结束时必须改变至少一项:          │
│         信息/目标/权力/关系/代价                               │
│     卡4 目标因果: 每个关键场景必须回答:                        │
│         角色想要什么? 什么阻止了他? 什么改变了?                │
│     卡5 情感评估: 情绪必须改变角色注意力或行动，               │
│         不要只标注感受 (ban: "他感到悲伤")                     │
│     卡6 母题回归: 重复出现的意象必须每次携带新信息             │
│     卡7 句法节奏: 句长由场景压力驱动，                         │
│         紧张→短句，舒缓→长句，ban随机交替                     │
│     卡8 自由间接引语: 何时让叙述融入角色声音的规则             │
│     卡9 对话潜台词: 表面话题 vs 真实目的必须有压力差           │
│                                                              │
│     ★ v3.0 写作硬规则:                                      │
│     · POV 统一: 单章内不切换叙事视角                          │
│     · 角色不可崩: "宁可平淡也不可失真"                        │
│     · 伏笔必须隐蔽: 用情节暗示，不用直接说明                  │
│     · 对话: 口语化、带性格、推动情节，不灌水                  │
│     · 动作: 具体物理动作，ban "他想道"/"她觉得"                │
│     · 环境: 服务情绪，1-2 笔细节即可，不铺排                  │
│     · 内心独白: 短而有力，不超过 3 句连续                     │
│     · 开头 300 字: 必须有在场异常+角色行动+主观判断            │
│                                                              │
│     输出: 章节正文初稿 (3000-5000 字)                        │
│                                                              │
│  ④ 编辑 Editor                                              │
│     ★ v3.0 升级为冲突仲裁模式:                              │
│     输入: 初稿 + 风格指南 + beat sheet                       │
│     审查维度:                                                 │
│     · 事实层: 与 Wiki 页面是否矛盾 → 最高优先级              │
│     · 风格层: 对话比例(≤40%)、段落长度(≤200字)、              │
│       感官细节(每500字至少1处)                                 │
│     · 节奏层: 是否符合微弧 4 段式比例                         │
│     · 衔接层: 场景边界是否平滑、有无 POV 跳转                 │
│     冲突仲裁优先级:                                          │
│     · 事实冲突: 设定 > 风格 (事实不可谈判)                    │
│     · 风格争议: 风格 > 建筑师 (风格拥有美感控制权)            │
│     · 节奏争议: 建筑师 > 风格 (建筑师控制节奏)               │
│     输出: 修订稿                                              │
│                                                              │
│  ⑤ 去 AI 味 Humanizer                                       │
│     ★ v3.0 升级为三层检测 (词表级+语义级+计算级):             │
│     (详见 5.5 节)                                             │
│     输出: 终稿                                               │
│                                                              │
│  ⑥ 观察者 Observer                                          │
│     ★ v3.0: 拆为 3 路并行 LLM 调用:                         │
│     · 社会维: 角色状态/关系/情感/体貌变化                     │
│     · 环境维: 地点/资源/时间推进                              │
│     · 叙事维: 信息揭示/伏笔埋设与回收/弧线进度               │
│     3 路结果合并为 CHANGES 声明                               │
│                                                              │
│  ⑦ 验证者 Verifier                                          │
│     独立调用，交叉验证 Observer 的 CHANGES                    │
│     (v2.0 已有，不变)                                         │
│                                                              │
│  ⑧ Wiki 更新 (新增，零 Agent 成本)                           │
│     读取合并后的 CHANGES → 自动 ingest 到 L1 活体故事 Wiki    │
│     更新涉及实体的 Wiki 页面 + 维护 [[wikilink]]              │
└──────────────────────────────────────────────────────────────┘
```

### 5.3 质量门控（v3.0: 6 门 → 9 门）

九道质量门覆盖格式、叙事、实体、状态、伏笔和世界规则：

```
门控 1 - 大纲合规: beat sheet 覆盖章纲要求的所有事件
门控 2 - 连续性通过: Guardian 零 FAIL 项
门控 3 - 字数达标: 3000-5000 字，不灌水

★ 门控 4 - 实体引用验证:
  正文中出现的每个实体名必须在 Wiki 中有对应页面。
  · 新出场实体 → 允许，但必须在 CHANGES 中声明 "new_entity"
  · 超过 5 个未注册实体 → FAIL
  · 超过 3 个"龙套"级未注册实体 → 警告

★ 门控 5 - 聚焦化边界检查 (叙事学卡1):
  视角人物不能感知/读取他人内心。
  代码检测: 扫描"他心想""她暗道"等内心描写标记，
  检查主语是否为非视角人物 → 违规=硬 FAIL

门控 6 - 伏笔一致: 本章操作的伏笔状态与伏笔池匹配

★ 门控 7 - 世界规则约束:
  禁忌规则 Wiki 页面中的每条硬约束 (如"凡人不可飞行"),
  用否定模式匹配扫描正文:
  · 跳过被否定前缀修饰的出现 (如"无法飞行"不算违规)
  · 匹配到 → FAIL + 标注具体位置

门控 8 - 去 AI 味得分: 计算级指标全部达标（详见 5.5）
门控 9 - 风格一致: 与作者风格指纹的余弦相似度 > 0.85

未通过 → 重试（最多 3 次）→ 标记为需人工审核
```

### 5.4 风格蒸馏系统（v3.0 新增）

系统允许用户上传其有权处理的参考文本，并自动提取风格指纹。

```
用户上传参考小说 (.txt)
        │
        ▼
分块: 5000 字/块，最多取 20 块（均匀分布全书）
        │
        ▼
6 维并行 LLM 分析:
  ┌→ 写作风格: 句式特征、用词偏好、修辞手法
  ├→ 世界构建: 设定展开方式、信息投放节奏
  ├→ 角色塑造: 内心描写方式、对话风格、行为模式
  ├→ 情节结构: 章节开合节奏、转折密度、悬念手法
  ├→ 伏笔手法: 埋伏笔密度、回收跨度、暗示方式
  └→ 叙事结构: POV 切换规则、多线编织方式、张力弧线
        │
        ▼
合成: NarrativeStrategyProfile
  {
    chapter_function_patterns: [...],  // 章节类型模式
    pov_switching_rules: "...",        // 视角切换规则
    foreshadowing_density: 0.3,        // 每章平均伏笔数
    tension_arc_templates: [...],      // 张力弧线模板
    dialogue_style: "...",             // 对话风格特征
    description_style: "...",          // 描写风格特征
    banned_patterns: [...]             // 该作者不用的模式
  }
        │
        ▼
注入: 写入 style_references 表，
      Writer/Editor prompt 中作为"风格指南"注入
```

### 5.5 去 AI 味系统（v3.0: 词表级 → 三层检测）

> v2.0 只有 40+ 词表模式检测。v3.0 升级为词表检测、语义检测和计算指标三层，
> 并通过上下文感知选择改写候选。

```
┌─────────────────────────────────────────────────────────────┐
│  第一层：词表级检测 + 上下文感知替换                          │
│                                                             │
│  44 类疲劳词，按类别分组:                                   │
│  · 总结词: "总之""综上所述""不难发现"                        │
│  · 对冲词: "不禁""忍不住""情不自禁"                         │
│  · AI 口味: "值得一提的是""某种程度上""赋能""闭环"           │
│  · 情绪标签: "心头一震""不由心中一凛""眼中闪过一丝"          │
│  · 过渡套话: "与此同时""话虽如此""正当此时"                  │
│                                                             │
│  8 类禁止句式 (正则匹配):                                    │
│  · "这一切让他明白了..."                                    │
│  · "一是...二是...三是..."                                   │
│  · "不是X而是Y"                                             │
│  · "随着...的不断..."                                       │
│  · "首先...其次...最后..."                                   │
│  · 连续破折号超 4 个/章                                      │
│  · 连续感叹号/问号超 2 个                                    │
│  · "如同/宛如/仿佛" 超 2 次/章                               │
│                                                             │
│  替换策略:                                                   │
│  · 100% 强制替换: AI 论文腔 (赋能→帮助, 底层逻辑→根本原因)  │
│  · 85% 替换: 过渡词多样化 (因此→所以/故/于是/由此)           │
│  · 50% 替换: 低频词增加变化                                  │
│  · 上下文感知: 用 NGram 评分从候选列表中选最佳替换,          │
│    而不是随机选择                                            │
│                                                             │
│  改写样例对:                                                 │
│  bad:  "总之，这一战让他明白了自己的不足"                     │
│  good: "他擦掉嘴角的血，盯着对手远去的背影，拳头攥得发白"     │
│  原则: "用动作和细节代替总结性陈述"                           │
│                                                             │
│  数字规范化:                                                 │
│  · 阿拉伯数字→中文数字 (2024年→二〇二四年, 12月→十二月)     │
└─────────────────────────────────────────────────────────────┘
                         │
┌─────────────────────────────────────────────────────────────┐
│  第二层：语义级检测 (LLM 检查)                               │
│                                                             │
│  7 类语义 AI 味:                                             │
│                                                             │
│  · 结构 AI 味: 三段式排比、对称句式堆叠、散文中的编号小标题  │
│  · 描写 AI 味: 抽象标签替代五感("环境很压抑" → 应给出        │
│    触觉/嗅觉/听觉细节)、情绪标签替代身体反应                 │
│  · 对话 AI 味: 角色同质化(声音不可区分)、过度解释动机、       │
│    说明书语气                                                │
│  · 节奏 AI 味: 一切都解释完毕(不留想象空间)、                │
│    章末强行升华、抽象反问句做钩子                             │
│                                                             │
│  检测方式: 每 5 章做一次 LLM 语义审查，                       │
│  发现问题生成改写建议注入下一轮 Writer prompt                 │
└─────────────────────────────────────────────────────────────┘
                         │
┌─────────────────────────────────────────────────────────────┐
│  第三层：计算级检测 (零 LLM 成本, 代码计算)                  │
│  ★ v3.0 新增 4 项计算指标                                    │
│                                                             │
│  指标 1 - 突发性 (Burstiness):                               │
│    句长标准差。真人写作句长变化大，AI 倾向均匀。              │
│    计算: std(sentence_lengths) / mean(sentence_lengths)      │
│    阈值: > 0.4 合格 (真人通常 0.5-0.8, AI 通常 0.2-0.3)     │
│                                                             │
│  指标 2 - 字符熵 / TTR (Type-Token Ratio):                   │
│    用词丰富度。AI 倾向重复使用同一组高频词。                  │
│    计算: unique_chars / total_chars (中文按字计算)            │
│    阈值: > 0.35 合格                                         │
│                                                             │
│  指标 3 - 语义困惑度 (Semantic Perplexity):                   │
│    连续句子执行相同语义功能的比率。                           │
│    AI 倾向连续写 "描写→描写→描写"，                          │
│    真人写作功能交替 "描写→对话→动作→内心"。                  │
│    计算: 对每句标注语义功能(描写/对话/动作/内心/叙述),        │
│    统计连续同功能句的占比                                     │
│    阈值: < 30% 合格                                          │
│                                                             │
│  指标 4 - 结构指纹 (Structure Fingerprint):                  │
│    模板连接词密度。                                           │
│    统计: "首先/其次/最后/因此/然而/不过/总之/由此可见"        │
│    等连接词在全文中的出现频率                                 │
│    阈值: < 0.5% 合格 (占总字数比例)                          │
│                                                             │
│  任一指标不合格 → 触发 Humanizer 重处理 (最多 2 轮)          │
└─────────────────────────────────────────────────────────────┘
```

### 5.6 DeerFlow Skill 定义（v3.0 扩展）

将写作流水线封装为 DeerFlow 的 Skill：

```
skills/
├── novel_architect.md       # 建筑师: 微弧节奏 + beat sheet
├── novel_writer.md          # 写手: 9 卡叙事学协议 + 写作硬规则  
├── novel_editor.md          # 编辑: 仲裁者模式 + 优先级层级
├── novel_guardian.md         # 连续性守卫: 10 项代码检查 + LLM 审核
├── novel_humanizer.md        # 去 AI 味: 三层检测(词表+语义+计算)
├── novel_observer.md         # 观察者: 3 路并行(社会/环境/叙事)
├── novel_verifier.md         # 验证者: CHANGES 交叉验证
├── wiki_ingest.md            # ★ Wiki 自动更新
├── wiki_lint.md              # ★ Wiki 一致性审计
├── worldbuilding.md          # 世界构建
├── outline_generator.md      # 大纲生成
├── style_distiller.md        # ★ 风格蒸馏(6维分析)
└── toc_builder.md            # ★ TOC 树构建/增量更新
```

每个 Skill 的 Markdown 结构：
```markdown
# 角色名

## 身份
你是...

## 输入格式
{JSON Schema}

## 输出格式
{JSON Schema}

## 规则（必须遵守）
1. ...
2. ...

## 禁止（违反=重做）
- ...

## 示例
输入: ...
输出: ...
```

关键 Skill prompt 片段（v3.0 核心改进）：

**architect.md** 中的微弧指令:
```
你必须为每章设计 4 段式微弧:
- 开场触发 (占全章 15-20%): 必须自然承接上一章最后的场景/动作/对白
- 推进冲突 (40-50%): 至少包含 2 个冲突——1 个外部阻力 + 1 个内心价值冲突
- 逆转/新信息 (20-25%): 一个改变局势的具体事件
- 余波/钩子 (10-15%): 情感落点 + 下章悬念

禁止:
- 不得输出 "开场建立章节目标" 这类元叙事
- 每个 beat 必须是"具体事件"(有角色动作 + 情势变化)
- 不得在单章内解决所有核心矛盾
```

**writer.md** 中的 9 卡叙事学注入:
```
写作时必须遵守以下 9 条叙事学约束:

1. [聚焦化边界] 你只能描写视角人物能感知的事物。
   不得描写他人内心想法。违反此规则=硬错误。
2. [心理距离] 重要决定/受伤/发现 → 切近(详写内心)
   跨时间背景 → 拉远(概述)
3. [场景终止条件] 每个场景结束时必须改变至少一项:
   信息/目标/权力格局/人物关系/代价
...
(完整 9 卡注入 Writer prompt)
```

---

## 六、数据库设计

### 6.1 数据库选择

在现有 PostgreSQL 实例上新建 `fushengliunian` 库，安装 `pgvector` 扩展用于向量检索。

### 6.2 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    nickname VARCHAR(50) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT now()
);

-- 小说项目表
CREATE TABLE projects (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    title VARCHAR(200) NOT NULL,
    genre VARCHAR(50) NOT NULL,           -- 玄幻/都市/科幻/言情...
    one_sentence TEXT NOT NULL,           -- 一句话概述
    target_words INTEGER DEFAULT 1000000, -- 目标字数
    status VARCHAR(20) DEFAULT 'draft',   -- draft/writing/paused/completed
    style_profile JSONB,                  -- 风格指纹配置
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
);

-- ★ v3.0: 活体故事 Wiki 表 (替代原 story_bible)
-- 每个实体一个 Wiki 页面，用 wikilinks 互相引用
CREATE TABLE story_wiki (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    slug VARCHAR(200) NOT NULL,            -- URL 友好的唯一标识 (如 "li-xiaoyao")
    category VARCHAR(50) NOT NULL,         -- character/location/faction/worldview/canon_rule/timeline
    title VARCHAR(200) NOT NULL,           -- 显示名 (如 "李逍遥")
    content TEXT NOT NULL,                 -- Markdown 格式，含 [[wikilink]]
    aliases TEXT[],                        -- 别名列表，用于 Aho-Corasick 匹配
    wikilinks TEXT[],                      -- 本页引用的其他页面 slug 列表
    last_updated_chapter INTEGER DEFAULT 0,-- 最后被 ingest 更新的章节号
    source_chapters INTEGER[],             -- 内容来源的章节号列表
    visibility VARCHAR(20) DEFAULT 'active', -- active/reference/hidden
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now(),
    UNIQUE(project_id, slug)
);

-- Wiki 入链索引 (用于 lint 孤页检测)
CREATE INDEX idx_wiki_wikilinks ON story_wiki USING gin (wikilinks);
CREATE INDEX idx_wiki_category ON story_wiki (project_id, category);

-- ★ v3.0: TOC 树表 (PageIndex 模式)
-- 小说的层级目录索引，用于 LLM 推理导航
CREATE TABLE novel_toc (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    parent_id UUID REFERENCES novel_toc(id),   -- null=书级根节点
    level VARCHAR(20) NOT NULL,                -- book/volume/arc/chapter
    sequence INTEGER NOT NULL,                 -- 同级排序
    title VARCHAR(200) NOT NULL,
    summary TEXT,                              -- ≤200字摘要 (LLM 生成)
    characters TEXT[],                         -- 涉及角色
    key_events TEXT[],                         -- 关键事件
    chapter_range_start INTEGER,               -- 起始章节号
    chapter_range_end INTEGER,                 -- 结束章节号
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_toc_parent ON novel_toc (project_id, parent_id);
CREATE INDEX idx_toc_range ON novel_toc (project_id, chapter_range_start, chapter_range_end);

-- 大纲表（三级：书→卷→章）
CREATE TABLE outlines (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    parent_id UUID REFERENCES outlines(id), -- null=书级, 有值=卷级或章级
    level VARCHAR(10) NOT NULL,            -- book/volume/chapter
    sequence INTEGER NOT NULL,             -- 排序号
    title VARCHAR(200),
    content JSONB NOT NULL,                -- 大纲内容
    is_sealed BOOLEAN DEFAULT FALSE,       -- 锁定后不可修改
    created_at TIMESTAMPTZ DEFAULT now()
);

-- 状态变更日志表 (L2) - append-only，永不覆写
-- ★ v2.0 改为日志模式，防止级联崩溃
CREATE TABLE state_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    chapter_sequence INTEGER NOT NULL,     -- 产生变更的章节号
    dimension VARCHAR(50) NOT NULL,        -- character_state/position/conflict/...
    entity_name VARCHAR(200) NOT NULL,     -- 实体名称
    field VARCHAR(100),                    -- 字段名（如"修为""位置"）
    old_value TEXT,
    new_value TEXT,
    confidence REAL DEFAULT 0.9,           -- 0-1，双重提取一致=0.9，仅单方=0.5
    source VARCHAR(20) DEFAULT 'observer', -- observer/verifier/manual/audit
    created_at TIMESTAMPTZ DEFAULT now()
);

CREATE INDEX idx_state_log_entity ON state_log(project_id, entity_name, field);
CREATE INDEX idx_state_log_chapter ON state_log(project_id, chapter_sequence);

-- 当前状态物化视图（从日志计算，每章生成后刷新）
CREATE MATERIALIZED VIEW current_state AS
SELECT DISTINCT ON (project_id, dimension, entity_name, field)
    project_id, dimension, entity_name, field, new_value AS current_value,
    chapter_sequence AS last_updated_chapter, confidence
FROM state_log
ORDER BY project_id, dimension, entity_name, field, chapter_sequence DESC;

-- 实体温度表（冷热分层）
CREATE TABLE entity_temperature (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    entity_name VARCHAR(200) NOT NULL,
    entity_type VARCHAR(50) NOT NULL,      -- character/location/faction/conflict
    temperature VARCHAR(10) DEFAULT 'hot', -- hot/warm/cold/frozen
    last_referenced_chapter INTEGER NOT NULL,
    compressed_summary TEXT,               -- 冷区实体的压缩摘要
    UNIQUE(project_id, entity_name)
);

-- 章节表
CREATE TABLE chapters (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    volume_sequence INTEGER NOT NULL,
    chapter_sequence INTEGER NOT NULL,      -- 全书章节序号
    title VARCHAR(200),
    content TEXT,                           -- 正文
    summary TEXT,                           -- 结构化摘要
    word_count INTEGER,
    status VARCHAR(20) DEFAULT 'pending',   -- pending/generating/draft/confirmed/failed
    quality_scores JSONB,                   -- 各维度质量评分
    beat_sheet JSONB,                       -- 场景拍
    generation_log JSONB,                   -- 生成过程日志
    created_at TIMESTAMPTZ DEFAULT now(),
    updated_at TIMESTAMPTZ DEFAULT now(),
    UNIQUE(project_id, chapter_sequence)
);

-- 伏笔池（v2.0: 增加优先级和重要度）
CREATE TABLE foreshadowing (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    content TEXT NOT NULL,                  -- 伏笔内容
    planted_chapter INTEGER NOT NULL,       -- 埋设章节
    target_chapter INTEGER,                 -- 预计回收章节（可空）
    resolved_chapter INTEGER,               -- 实际回收章节（可空）
    importance VARCHAR(1) DEFAULT 'B',      -- S/A/B 重要度分级
    status VARCHAR(20) DEFAULT 'active',    -- active/resolved/abandoned/escalated
    escalation_count INTEGER DEFAULT 0,     -- 被强制升级的次数
    related_characters TEXT[],              -- 关联角色
    created_at TIMESTAMPTZ DEFAULT now()
);

-- ★ 活跃伏笔不超过 50 条的约束由应用层保证

-- 角色认知表（谁知道什么）
CREATE TABLE character_knowledge (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    character_name VARCHAR(100) NOT NULL,
    knowledge TEXT NOT NULL,                -- 知道的事实
    source_chapter INTEGER,                 -- 获知章节
    knowledge_type VARCHAR(20) DEFAULT 'knows', -- knows/suspects/reader_only
    created_at TIMESTAMPTZ DEFAULT now()
);

-- 章节向量索引 (v2.0: 增加元数据标签用于预过滤)
CREATE TABLE chapter_chunks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    chapter_sequence INTEGER NOT NULL,
    chunk_index INTEGER NOT NULL,
    content TEXT NOT NULL,
    entities TEXT[],                        -- 涉及的实体名
    arc_id VARCHAR(100),                    -- ★ 所属弧线/卷标识
    is_milestone BOOLEAN DEFAULT FALSE,     -- ★ 是否为关键转折点
    embedding vector(1024),                -- 向量 (用 pgvector)
    created_at TIMESTAMPTZ DEFAULT now()
);

-- ★ 用 HNSW 替代 ivfflat（小数据集上更准确，不需要训练）
CREATE INDEX idx_chunks_embedding ON chapter_chunks 
    USING hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64);

-- 全文搜索索引
CREATE INDEX idx_chunks_fts ON chapter_chunks 
    USING gin (to_tsvector('simple', content));

-- ★ 元数据过滤索引（检索时先按角色/弧线/章节范围过滤）
CREATE INDEX idx_chunks_entities ON chapter_chunks USING gin (entities);
CREATE INDEX idx_chunks_arc ON chapter_chunks (project_id, arc_id);
CREATE INDEX idx_chunks_chapter ON chapter_chunks (project_id, chapter_sequence);

-- 写作风格库（拆书用）
CREATE TABLE style_references (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
    source_name VARCHAR(200) NOT NULL,     -- 参考作品名
    style_profile JSONB NOT NULL,          -- 风格分析结果
    sample_passages TEXT[],                -- 典型段落
    created_at TIMESTAMPTZ DEFAULT now()
);
```

---

## 七、前端设计

### 7.1 设计原则

1. **极简**：一屏只做一件事
2. **引导**：每一步都有清晰的提示语，用大白话
3. **渐进**：先简单后复杂，新手可以全程用默认值
4. **实时反馈**：生成过程有进度条和预览

### 7.2 页面结构（共 5 个页面）

```
┌─────────────────────────────────────────────────────┐
│  页面 1: 登录/注册                                   │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │          浮 生 流 年                          │    │
│  │       AI 帮你写长篇小说                       │    │
│  │                                             │    │
│  │  ┌─────────────┐  ┌──────────────┐          │    │
│  │  │  手机号登录  │  │  微信扫码登录 │          │    │
│  │  └─────────────┘  └──────────────┘          │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  页面 2: 我的书架                                    │
│                                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐                        │
│  │      │ │      │ │  ＋  │                        │
│  │ 书1  │ │ 书2  │ │ 新书 │                        │
│  │      │ │      │ │      │                        │
│  │ 12万 │ │  3万 │ │      │                        │
│  │ 写作中│ │ 已完│ │      │                        │
│  └──────┘ └──────┘ └──────┘                        │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  页面 3: 创建新书向导（4 步）                        │
│                                                     │
│  步骤指示: ● ─ ○ ─ ○ ─ ○                           │
│                                                     │
│  第 1 步：选类型                                     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐              │
│  │ 玄幻 │ │ 都市 │ │ 科幻 │ │ 言情 │              │
│  └──────┘ └──────┘ └──────┘ └──────┘              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐              │
│  │ 历史 │ │ 悬疑 │ │ 游戏 │ │ 其他 │              │
│  └──────┘ └──────┘ └──────┘ └──────┘              │
│                                                     │
│  第 2 步：讲你的故事                                 │
│  "用一两句话说说你想写什么故事，                      │
│   比如：一个普通少年意外获得上古传承，                │
│   在修仙世界中一步步走向巅峰"                        │
│  ┌─────────────────────────────────┐                │
│  │                                 │                │
│  │   （大输入框）                   │                │
│  │                                 │                │
│  └─────────────────────────────────┘                │
│                                                     │
│  第 3 步：认识你的主角                               │
│  名字: ┌──────────┐  性别: ○男 ○女                  │
│  性格: ┌──────────────────────────┐                 │
│        │ 例如：表面沉稳内心热血    │                 │
│        └──────────────────────────┘                 │
│  ▸ 想加更多角色？（可选，展开）                       │
│                                                     │
│  第 4 步：写多长？                                   │
│  ○ 试试水 (30万字)                                  │
│  ● 正常长度 (100万字)                               │
│  ○ 超长篇 (300万字)                                │
│                                                     │
│  ┌─────────────┐                                    │
│  │  开始创建 →  │                                    │
│  └─────────────┘                                    │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  页面 4: 写作工作台（主界面）                        │
│                                                     │
│  ┌─── 左栏 30% ───┐┌──── 中栏 50% ────┐┌─右栏20%─┐│
│  │                ││                  ││         ││
│  │  卷一 □□□□□□  ││  第 42 章         ││ 本章信息││
│  │   第1章 ✓     ││  凌云剑试          ││         ││
│  │   第2章 ✓     ││                   ││ 出场角色││
│  │   第3章 ✓     ││  清晨的阳光穿过   ││ · 李逍遥││
│  │   ...         ││  竹林的缝隙……     ││ · 灵儿  ││
│  │  卷二 □□      ││                   ││         ││
│  │  ▸ 第40章 ✓  ││  （正文区域）      ││ 伏笔    ││
│  │  ▸ 第41章 ✓  ││                   ││ · 古符文││
│  │  ▸ 第42章 ✍  ││                   ││   埋:35章││
│  │  ▸ 第43章 ⏳  ││                   ││         ││
│  │               ││                   ││ 地点    ││
│  │ ┌───────────┐ ││                   ││ 万妖山  ││
│  │ │▶ 继续写作 │ ││                   ││         ││
│  │ └───────────┘ ││                   ││ 时间    ││
│  │ ┌───────────┐ ││                   ││ 灵历1342││
│  │ │⏸ 暂停     │ ││                   ││ 秋九月  ││
│  │ └───────────┘ ││                   ││         ││
│  │ ┌───────────┐ ││ ┌───┐┌───┐┌───┐  ││         ││
│  │ │↩ 重写本章 │ ││ │确认││重写││编辑│  ││         ││
│  │ └───────────┘ ││ └───┘└───┘└───┘  ││         ││
│  └───────────────┘└──────────────────┘└─────────┘│
│                                                     │
│  底部状态栏:                                         │
│  已写 12.3 万字 │ 第 42/300 章 │ 预计完成: 8小时     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  页面 5: 设定管理（从工作台进入）                     │
│                                                     │
│  标签页: [角色] [世界观] [大纲] [伏笔] [时间线]      │
│                                                     │
│  角色标签页:                                         │
│  ┌──────────────────────────────────────────┐       │
│  │ 李逍遥 ✏️                                │       │
│  │ 修为: 筑基六层  位置: 万妖山              │       │
│  │ 身上物品: 碧火剑, 储物袋                  │       │
│  │ 关系: 灵儿(恋人), 酒剑仙(师父)           │       │
│  │                                          │       │
│  │ 最近变化 (第42章):                        │       │
│  │ · 修为从筑基三层提升到六层                 │       │
│  │ · 获得碧火剑                              │       │
│  └──────────────────────────────────────────┘       │
│  ┌──────────────────────────────────────────┐       │
│  │ 灵儿 ✏️                                  │       │
│  │ ...                                      │       │
│  └──────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────┘
```

### 7.3 交互设计要点

**1. 向导引导（最关键）**

用户只需要回答 4 个问题，系统自动：
- 根据类型加载对应的提示词模板和风格规范
- 用"一句话"生成世界观、角色卡、势力体系
- 自动生成 30-100 卷大纲
- 全部后台完成，用户等待时看一个有趣的加载动画

**2. 写作过程零操作**

- 点击"继续写作"后自动逐章生成
- 每章生成完成后自动滚动显示
- 生成过程中显示当前步骤（"正在构思..."→"正在写作..."→"正在润色..."）
- 出问题自动重试，不需要用户操作

**3. 可选的深度控制**

进阶用户可以：
- 手动修改大纲
- 编辑角色卡
- 调整写作风格参数
- 上传有权处理的参考小说并提取风格指纹
- 手动编辑已生成的章节

**4. 移动端适配**

- 左栏章节列表默认收起，抽屉式展开
- 右栏信息面板默认收起
- 中栏正文占满全屏
- 底部固定操作按钮

### 7.4 技术选型

```
框架: Next.js 15 (App Router)
UI:   Tailwind CSS + shadcn/ui
状态: zustand
请求: 原生 fetch + SSE (EventSource)
编辑: 简单 textarea，不用富文本编辑器
部署: 静态导出 (next export) → Nginx 直接托管
```

选择静态导出是因为服务器内存紧张，不跑 Node.js 进程。API 调用走 FastAPI 后端。

---

## 八、API 设计

### 8.1 核心接口

```
POST   /api/auth/register          # 注册
POST   /api/auth/login             # 登录
GET    /api/auth/me                # 当前用户信息

GET    /api/projects               # 项目列表
POST   /api/projects               # 创建项目（触发世界构建）
GET    /api/projects/:id           # 项目详情
DELETE /api/projects/:id           # 删除项目

GET    /api/projects/:id/bible     # 故事圣经
PUT    /api/projects/:id/bible/:eid # 修改设定

GET    /api/projects/:id/outlines  # 大纲树
PUT    /api/projects/:id/outlines/:oid # 修改大纲

GET    /api/projects/:id/chapters          # 章节列表
GET    /api/projects/:id/chapters/:seq     # 章节详情
PUT    /api/projects/:id/chapters/:seq     # 手动编辑章节
POST   /api/projects/:id/chapters/:seq/confirm  # 确认章节
POST   /api/projects/:id/chapters/:seq/rewrite  # 重写章节

POST   /api/projects/:id/generate/start    # 开始生成
POST   /api/projects/:id/generate/pause    # 暂停生成
GET    /api/projects/:id/generate/status    # 生成状态

GET    /api/projects/:id/snapshot          # 当前状态快照
GET    /api/projects/:id/foreshadowing     # 伏笔列表
GET    /api/projects/:id/timeline          # 时间线

GET    /api/projects/:id/export/txt        # 导出 TXT
GET    /api/projects/:id/export/epub       # 导出 EPUB

# SSE 端点（实时推送生成进度）
GET    /api/projects/:id/stream            # SSE 流
```

### 8.2 SSE 事件类型

```
event: generation_start
data: {"chapter_sequence": 42, "step": "architect"}

event: generation_progress  
data: {"chapter_sequence": 42, "step": "writer", "progress": 60}

event: generation_chunk
data: {"chapter_sequence": 42, "text": "清晨的阳光..."}  # 流式输出正文

event: generation_complete
data: {"chapter_sequence": 42, "word_count": 4200, "quality_scores": {...}}

event: generation_error
data: {"chapter_sequence": 42, "error": "连续性检查未通过", "retry": 2}

event: state_update
data: {"chapter_sequence": 42, "changes": {...}}  # 状态变更通知
```

---

## 九、部署方案

### 9.1 服务器资源规划

```
当前占用（估算）：
- Nginx:       ~20MB
- PostgreSQL:  ~100MB
- 系统预留:    ~300MB
- guagua API:  ~100MB（可按需停）
总计约: ~520MB

fushengliunian 可用: ~1.4GB（停 guagua 后约 1.5GB）

fushengliunian 服务进程:
- FastAPI + DeerFlow 引擎: ~300-400MB
- 前端: 0MB（静态文件，Nginx 托管）
总计新增: ~400MB

留余: ~1GB — 够用，但要注意:
- 不要在服务器跑 embedding 模型
- 向量生成调用外部 API（DeepSeek embedding 或阿里通义千问 embedding）
- 批量生成时限制并发 LLM 调用数 ≤ 2
```

### 9.2 部署步骤

```bash
# 1. 新建数据库
sudo -u postgres createdb fushengliunian
sudo -u postgres psql -d fushengliunian -c "CREATE EXTENSION vector;"
sudo -u postgres psql -d fushengliunian -c "CREATE USER fushengliunian_app WITH PASSWORD '<生成强密码>';"
sudo -u postgres psql -d fushengliunian -c "GRANT ALL ON DATABASE fushengliunian TO fushengliunian_app;"

# 2. 部署后端
mkdir -p /opt/fushengliunian
# 上传代码，创建 venv，安装依赖
# 创建 systemd service

# 3. 部署前端
# 本地 next build && next export
# 上传到 /opt/fushengliunian/web

# 4. Nginx 配置
# novel.example.com → 前端静态 + /api 反代到 :8100

# 5. DNS
# 添加 A 记录: novel → 服务器公网 IP
```

### 9.3 Nginx 配置

```nginx
server {
    listen 80;
    server_name novel.example.com;

    # 前端静态文件
    root /opt/fushengliunian/web;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反代
    location /api/ {
        proxy_pass http://127.0.0.1:8100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # SSE 支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }
}
```

---

## 十、开发分期

### Phase 1: 最小可用 (2 周)

**目标**: 能用向导创建项目，自动生成大纲，逐章生成正文

- [ ] 后端框架搭建（FastAPI + DeerFlow 集成）
- [ ] 数据库建表 + 基础 CRUD
- [ ] 世界构建 Skill（一句话 → 世界观 + 角色卡 + 大纲）
- [ ] 章节生成 Skill（简化版：Architect → Writer → Observer）
- [ ] L2 状态快照基础版（角色状态 + 位置 + 时间线）
- [ ] 前端向导页 + 书架 + 基础工作台
- [ ] SSE 流式输出
- [ ] 部署到服务器

### Phase 2: 质量提升 (2 周)

**目标**: 写出来的内容质量像真人

- [ ] 完整 6 Agent 流水线
- [ ] 连续性引擎第一层（代码级 10 项检查）
- [ ] 连续性引擎第二层（Guardian Agent LLM 审核）
- [ ] 去 AI 味 Humanizer（40+ 模式检测 + 替换）
- [ ] CHANGES 双重提取（Observer + Verifier）
- [ ] Append-only 状态日志 + 物化视图
- [ ] 伏笔生命周期管理（50 条上限 + 优先级队列）
- [ ] 质量门控（6 关卡）

### Phase 3: 记忆完善 (2 周) ← 原 1 周，加固后需要 2 周

**目标**: 百万字不跑偏

- [ ] 冷热分层状态管理（热区 ≤ 50 实体 + 冷区归档）
- [ ] 场景相关性预筛选器（从 beat sheet 提取切片）
- [ ] 动态 Token 预算（8K-16K 按场景复杂度伸缩）
- [ ] L3 章节向量索引（pgvector + 元数据标签）
- [ ] 元数据预过滤混合检索系统
- [ ] 角色认知系统
- [ ] 每卷完成时自动压缩冷区状态
- [ ] 连续性引擎第三层（每 50 章深度审计）
- [ ] 级联崩溃修复流程（溯源 + 标记待审核章节）

### Phase 4: 体验打磨 (1 周)

**目标**: 用起来顺手

- [ ] 设定管理页面（角色/世界观/大纲/伏笔/时间线）
- [ ] 章节手动编辑 + 重写
- [ ] 状态修正界面（用户手动修正 CHANGES 遗漏）
- [ ] 导出 TXT/EPUB
- [ ] 移动端适配
- [ ] 拆书功能（上传参考小说 → 提取风格）
- [ ] 多 LLM 切换（DeepSeek/豆包/Claude）

---

## 十一、成本估算

### LLM API 调用成本（每章，v2.0 修订）

```
假设使用 DeepSeek V3（最便宜的高质量模型）：

v2.0 变化：新增 Verifier 双重提取 + 动态 Token 预算

每章平均上下文: ~12000 tokens 输入（动态预算中位数）
每章正文生成:   ~4000 tokens 输出

Agent 调用明细:
  Architect:  3000 in + 1500 out
  Guardian:   3000 in + 500 out
  Writer:     12000 in + 4000 out  (最大的调用)
  Editor:     6000 in + 4000 out
  Humanizer:  5000 in + 4000 out
  Observer:   5000 in + 1000 out
  Verifier:   5000 in + 1000 out   ★ 新增

单章总计:
  输入: ~39,000 tokens ≈ ¥0.04
  输出: ~16,000 tokens ≈ ¥0.02
  单章成本 ≈ ¥0.06

附加成本:
  每 50 章深度审计: ~100,000 tokens ≈ ¥0.2
  每卷冷区压缩:    ~50,000 tokens ≈ ¥0.1

100 万字 ≈ 250 章:
  章节生成: 250 × ¥0.06 = ¥15
  深度审计: 5 × ¥0.2 = ¥1
  冷区压缩: 3 × ¥0.1 = ¥0.3
  总计 ≈ ¥16.3（比 v1.0 的 ¥7.5 翻倍，但可靠性大幅提升）

500 万字 ≈ 1250 章:
  章节生成: 1250 × ¥0.06 = ¥75
  深度审计: 25 × ¥0.2 = ¥5
  冷区压缩: 13 × ¥0.1 = ¥1.3
  总计 ≈ ¥81.3

使用 Claude (高质量模式):
  单章成本 ≈ ¥0.8
  100 万字 ≈ ¥200 + 审计 ¥25 ≈ ¥225
```

### 服务器成本

```
现有阿里云轻量服务器: 已有，不额外增加
域名: 已有
总额外成本: 仅 LLM API 费用
```

---

## 十二、技术风险与应对（v2.0 修订）

| 风险 | 严重度 | 影响 | 应对方案 |
|------|--------|------|---------|
| 状态快照膨胀超出 Context | **致命** | 200 章后系统"看不见"大部分世界 | 冷热分层（热区 ≤ 50 实体），场景相关性预筛选器 |
| CHANGES 遗漏级联崩溃 | **致命** | 错误状态被后续章节合法化并放大 | 双重提取 + append-only 日志 + 置信度评分 + 溯源修复 |
| 人设漂移 | **高** | 角色逐渐偏离初始设定 | 每 50 章深度审计，输出修正指令注入后续大纲 |
| 伏笔堆积 | **高** | 500+ 活跃伏笔无法注入 Context | 硬上限 50 条 + 优先级队列 + 到期强制升级 |
| Token 预算不足 | **高** | 复杂场景丢失关键上下文 | 动态 8K-16K 预算按场景复杂度伸缩 |
| 检索噪声（同题材） | **中** | 向量搜索返回不相关但语义相似的结果 | 元数据预过滤缩小候选集；实体匹配权重升至 1.2 |
| 服务器 2GB 内存不够 | **中** | 进程被 OOM Kill | DeerFlow 轻量化运行；并发限制 ≤ 2；必要时停 guagua |
| LLM 输出格式不稳定 | **中** | CHANGES 解析失败 | Pydantic 强制 JSON Schema；失败自动重试 3 次 |
| 去 AI 味效果不好 | **低** | 用户不满意 | 多模型对比；引入更多真人语料训练替换规则 |
| pgvector 在小服务器上慢 | **低** | 检索延迟高 | HNSW 索引 + 元数据预过滤减少候选集 |
| ICP 备案未通过 | **低** | 域名无法公网访问 | 临时用 IP 直接访问；或 Cloudflare Tunnel |

---

## 十三、项目目录结构

```
fushengliunian/
├── backend/                    # FastAPI + DeerFlow 后端
│   ├── app/
│   │   ├── main.py            # FastAPI 入口
│   │   ├── config.py          # 配置
│   │   ├── models/            # SQLAlchemy 模型
│   │   ├── api/               # API 路由
│   │   │   ├── auth.py
│   │   │   ├── projects.py
│   │   │   ├── chapters.py
│   │   │   ├── bible.py
│   │   │   └── generate.py
│   │   ├── engine/            # 写作引擎核心
│   │   │   ├── pipeline.py    # Agent 流水线编排
│   │   │   ├── memory.py      # 记忆引擎（冷热分层）
│   │   │   ├── context.py     # Context Pack 动态组装
│   │   │   ├── scene_slicer.py # ★ 场景相关性预筛选器
│   │   │   ├── guardian.py    # 连续性引擎（三层）
│   │   │   ├── auditor.py     # ★ 每50章深度审计
│   │   │   ├── humanizer.py   # 去 AI 味
│   │   │   ├── observer.py    # 状态观察者（双重提取）
│   │   │   ├── verifier.py    # ★ CHANGES 交叉验证
│   │   │   ├── state_log.py   # ★ Append-only 状态日志
│   │   │   ├── foreshadow.py  # ★ 伏笔生命周期管理
│   │   │   └── retrieval.py   # 混合检索（元数据预过滤）
│   │   ├── skills/            # DeerFlow Skills
│   │   │   ├── architect.md
│   │   │   ├── writer.md
│   │   │   ├── editor.md
│   │   │   ├── guardian.md
│   │   │   ├── humanizer.md
│   │   │   ├── observer.md
│   │   │   ├── worldbuilder.md
│   │   │   └── outliner.md
│   │   └── utils/
│   │       ├── llm_client.py  # LLM 调用封装
│   │       ├── embedding.py   # 向量生成
│   │       └── aho_corasick.py # 实体匹配
│   ├── requirements.txt
│   └── Dockerfile             # 可选，本地开发用
│
├── frontend/                   # Next.js 前端
│   ├── app/
│   │   ├── page.tsx           # 首页/登录
│   │   ├── bookshelf/         # 书架
│   │   ├── create/            # 创建向导
│   │   ├── workspace/[id]/    # 写作工作台
│   │   └── settings/[id]/     # 设定管理
│   ├── components/
│   │   ├── wizard/            # 向导组件
│   │   ├── editor/            # 编辑区
│   │   ├── sidebar/           # 章节列表
│   │   └── info-panel/        # 信息面板
│   ├── lib/
│   │   ├── api.ts             # API 调用
│   │   ├── sse.ts             # SSE 客户端
│   │   └── store.ts           # zustand store
│   ├── package.json
│   └── next.config.js
│
├── deploy/                     # 部署相关
│   ├── nginx.conf
│   ├── fushengliunian.service  # systemd
│   └── init_db.sql
│
├── 设计文档.md                  # 本文档
└── README.md
```

---

## 十四、可靠性压力测试报告

> 以下是对 v1.0 设计进行的压力测试结果和 v2.0 的修复措施。
> 测试基准：500 万字 / 1250 章 / 200 角色 / 500 伏笔。

### 问题 1：状态爆炸 — 原设计 200 章后崩溃

```
v1.0 的问题：
  500 章时 L2 快照量化估算：
  - 10 主角 × 5000 字状态 = 50K 字
  - 50 重要配角 × 1000 字 = 50K 字  
  - 140 龙套 × 200 字 = 28K 字
  - 500 情节线 × 200 字 = 100K 字
  - 势力/地点/时间线 = ~90K 字
  - 总计 ~320K 字 ≈ 500K tokens
  
  而 Context Pack 只有 8K tokens → 只能看到全部状态的 1.3%
  200 章后系统实质上在"瞎写"

v2.0 修复：
  冷热分层 → 热区始终 ≤ 50 实体 ≤ 3000 tokens
  场景预筛选 → 只注入本场景需要的状态切片
  每卷冷区压缩 → 长期内存占用可控
  
  ✅ 1250 章时热区仍然 ≤ 3000 tokens，可持续
```

### 问题 2：CHANGES 遗漏级联崩溃 — 最致命的风险

```
v1.0 的问题：
  LLM 结构化提取遗漏率 5-15%
  500 章 × 平均 10 个状态变更 = 5000 条变更
  预计遗漏 250-750 条
  
  级联效应：
  第 300 章遗漏"角色的剑断了"
  → 第 301 章基于错误状态写了用剑的场景
  → 第 302 章的 CHANGES 记录"角色在用剑"
  → 错误被合法化，永远无法自动修复

v2.0 修复：
  双重提取（Observer + Verifier）→ 遗漏率降至 1-3%
  Append-only 日志 → 错误可溯源到具体章节
  置信度评分 → 低置信度状态被引用时触发警告
  溯源修复流程 → 发现错误后可插入修正记录
  
  ✅ 500 章预计遗漏降至 50-150 条，且可修复
```

### 问题 3：Token 预算不足 — 复杂场景丢失上下文

```
v1.0 的问题：
  固定 8K tokens
  800 章时一个涉及 5 角色 + 3 伏笔的场景：
  角色状态 2500 + 伏笔 1500 + 近章摘要 2000 + 地点 1000 = 7000
  留给 RAG、风格指南、里程碑的只剩 1000 tokens

v2.0 修复：
  动态 8K-16K 按场景复杂度伸缩
  简单场景（≤3 角色）→ 8K 够用
  复杂场景（>6 角色 或 大战）→ 16K
  LLM API 成本增加约 30%，但质量显著提升
  
  ✅ 即使 1250 章的复杂场景也有足够预算
```

### 问题 4：伏笔地狱 — 无限堆积无法管理

```
v1.0 的问题：
  500+ 活跃伏笔，平均等待 200+ 章回收
  全部注入 Context Pack 需要 ~50K tokens
  读者也早忘了 200 章前的暗示

v2.0 修复：
  活跃上限 50 条
  优先级队列 (S/A/B 级)
  到期强制升级 → 写入 beat sheet
  超期 "回声场景" → 帮读者回忆
  极端超期 → 自动 abandoned
  
  ✅ Context Pack 中最多 10 条伏笔，可控
```

### 问题 5：人设漂移 — 代码检查无法发现

```
v1.0 的问题：
  代码级检查只抓机械错误（死角色/位置/物品）
  无法检测：
  - 角色性格渐变（冷酷变圣母）
  - 能力遗忘（有传送术却走路）
  - 反派降智（明显更好的选择不用）
  - 节奏退化（连续 50 章打怪升级）
  - 契诃夫之枪（引入的元素不使用）

v2.0 修复：
  三层检查:
  L1 代码级（每章，零成本）→ 抓机械错误
  L2 Guardian LLM（每章，低成本）→ 抓逻辑/人设问题
  L3 深度审计（每 50 章，用长上下文模型）→ 抓漂移/退化
  
  ✅ 500 章后人设漂移可被审计发现并修正
```

### 问题 6：检索退化 — 同题材向量噪声

```
v1.0 的问题：
  修仙小说中"突破""丹药""剑气"等高频词
  导致大量不相关但语义相似的分块被检索出来
  Top-10 中可能 3-4 个是噪声

v2.0 修复：
  元数据预过滤 → 先按角色/弧线/章节范围缩小候选集
  实体匹配权重从 0.95 升至 1.2
  向量匹配权重从 1.0 降至 0.8
  HNSW 索引替代 ivfflat（小数据集更准确）
  
  ✅ 候选集从 10000+ 缩小到 500-2000，噪声大幅降低
```

### 问题 7：级联崩溃传播 — 一章错全书错

```
v1.0 的问题：
  覆写式状态更新 → 无法回滚
  错误状态一旦写入就是"真相"
  5-10 章后错误被多次佐证，变成不可逆

v2.0 修复：
  Append-only 日志 → 可追溯到任何时间点的状态
  置信度字段 → 低置信度状态会触发警告
  修正记录 → 可在任意章节号插入修正
  受影响章节标记 → 不自动重写，交给用户决定
  
  ✅ 错误可被发现、定位、修复，不会无限传播
```

### 可靠性总结

```
                    v1.0          v2.0
支撑章节数          ~150-200      1250+
状态可见度(500章)   1.3%          85%+ (热区+按需召回)
CHANGES 遗漏率      5-15%         1-3% (双重提取)
级联崩溃风险        致命          可修复
人设漂移检测        无            每50章审计
伏笔管理能力        无上限(失控)  50条硬上限
每章 LLM 成本       ¥0.03        ¥0.06
500万字总成本       ~¥38          ~¥81
```

**结论**：v2.0 设计以约 2 倍的 LLM 成本换取了从"200 章崩溃"到"1250+ 章可持续"
的可靠性提升。对于 500 万字级别的长篇小说，这是一个可以工作的系统。

---

## 十五、尚存的已知局限

以下问题在当前设计中**没有完美解决**，需要持续迭代：

1. **风格一致性**：去 AI 味可以消除套路用语，但跨 1000 章的叙事声音一致性
   仍然是未解难题。目前靠风格指南 + 风格蒸馏缓解，但不保证完美。

2. **叙事弧线宏观控制**：大纲可以规划宏观走向，但 AI 在 500 章尺度上对
   "张弛有度""欲扬先抑"的掌控力不如人类作者。每 50 章审计可以发现问题，
   但自动修复能力有限。

3. **创意枯竭**：同一个模型写 1000+ 章后，情节桥段会趋于同质化。
   需要定期更换/混用 LLM 模型，或引入随机性种子。

4. **用户干预后的状态同步**：用户手动编辑章节内容后，CHANGES 声明
   需要重新生成，否则状态与正文不一致。这需要"编辑后自动重提取"功能。

5. **2GB 服务器的长期瓶颈**：PostgreSQL + pgvector 在 10000+ 向量分块时
   内存压力会上升。如果项目做大，需要升级服务器或将向量检索外置。

---

## 十六、总结

浮生流年 v2.0 = **傻瓜向导** + **冷热分层状态** + **双重提取** + **三层检查** + **7 Agent 流水线** + **去 AI 味**

核心差异化：
1. **冷热分层 + 场景预筛选**：不靠 LLM 记忆，靠结构化状态 + 智能裁剪，1250 章可持续
2. **双重提取 + append-only 日志**：状态遗漏从 15% 降至 3%，错误可溯源修复
3. **三层检查**：代码级（每章）+ Guardian LLM（每章）+ 深度审计（每 50 章），覆盖机械错误到人设漂移
4. **前端极简**：回答 4 个问题就能开始写书
5. **去 AI 味**：40+ 模式检测 + 风格指纹注入
6. **成本可控**：DeepSeek 主力，500 万字约 ¥81

不追求的：
1. 不做多人协作
2. 不做在线阅读平台
3. 不做 AI 聊天对话式写作
4. 第一版不做付费系统
